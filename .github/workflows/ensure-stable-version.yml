name: Ensure Stable Version on Main

on:
  push:
    branches: [main]
    paths:
      - 'src/version.py'

permissions:
  contents: write

jobs:
  strip-dev-suffix:
    runs-on: ubuntu-latest
    # Prevent infinite loop - only run for non-bot commits and non-strip-dev commits
    if: github.actor != 'github-actions[bot]' && !contains(github.event.head_commit.message, 'strip dev suffix')

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.CI_ADMIN_TOKEN }}
          ref: main

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Check version for dev suffix
        id: check_version
        run: |
          # Extract version with error checking
          if ! VERSION=$(python -c "from src.version import __version__; print(__version__)" 2>&1); then
            echo "ERROR: Failed to import version from src.version"
            echo "Output: $VERSION"
            exit 1
          fi

          if [ -z "$VERSION" ]; then
            echo "ERROR: Version string is empty"
            exit 1
          fi

          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Detected version: $VERSION"

          if [[ "$VERSION" == *"dev"* ]]; then
            echo "has_dev=true" >> $GITHUB_OUTPUT
            echo "Version $VERSION contains dev suffix - will strip it"
          else
            echo "has_dev=false" >> $GITHUB_OUTPUT
            echo "Version $VERSION is stable - no action needed"
          fi

      - name: Validate CI_ADMIN_TOKEN
        if: steps.check_version.outputs.has_dev == 'true'
        run: |
          if [ -z "${{ secrets.CI_ADMIN_TOKEN }}" ]; then
            echo "ERROR: CI_ADMIN_TOKEN secret is not set"
            echo "This workflow requires a Personal Access Token with admin:repo permissions"
            echo "Set it in repository Settings > Secrets > Actions"
            exit 1
          fi
          echo "CI_ADMIN_TOKEN is configured"

      - name: Strip dev suffix and push to main
        if: steps.check_version.outputs.has_dev == 'true'
        run: |
          # Strip dev suffix
          if ! uv run python scripts/bump_version.py --prerelease stable --no-git; then
            echo "ERROR: Failed to run bump_version.py"
            echo "Check script for errors or version.py format issues"
            exit 1
          fi

          # Verify version was actually changed
          if ! NEW_VERSION=$(python -c "from src.version import __version__; print(__version__)" 2>&1); then
            echo "ERROR: Failed to read new version after bump"
            exit 1
          fi

          if [ -z "$NEW_VERSION" ]; then
            echo "ERROR: New version string is empty"
            exit 1
          fi

      - name: Push and create PR
        if: steps.check_version.outputs.has_dev == 'true'
        id: create_pr
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          BRANCH="${{ steps.commit.outputs.branch }}"

          # Push branch with error handling
          if ! git push -u origin "$BRANCH"; then
            echo "ERROR: Failed to push branch $BRANCH to remote"
            exit 1
          fi

          # Check if PR already exists
          if ! EXISTING_PR=$(gh pr list --head "$BRANCH" --base main --json number,state --jq '.[0] // empty'); then
            echo "ERROR: Failed to check for existing PRs"
            exit 1
          fi

          if [ -n "$EXISTING_PR" ]; then
            PR_NUMBER=$(echo "$EXISTING_PR" | jq -r '.number')
            PR_STATE=$(echo "$EXISTING_PR" | jq -r '.state')

            if [ "$PR_STATE" != "OPEN" ]; then
              echo "PR #$PR_NUMBER is $PR_STATE, skipping auto-merge"
              exit 0
            fi

            echo "✓ PR #$PR_NUMBER already exists for branch $BRANCH"
          else
            if ! PR_NUMBER=$(gh pr create \
              --base main \
              --head "$BRANCH" \
              --title "chore: strip dev suffix for stable release ${{ env.new_version }}" \
              --body "Automated PR to strip \`.dev\` suffix from version on main branch.

          # Check if file actually changed
          if git diff --quiet src/version.py; then
            echo "ERROR: src/version.py has no changes despite version string changing"
            exit 1
          fi

          # Configure git
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          **Tests:**
          Tests are automatically skipped for this version-only change to save CI resources." \
              --json number --jq '.number'); then
              echo "ERROR: Failed to create PR for branch $BRANCH"
              exit 1
            fi

            if [ -z "$PR_NUMBER" ]; then
              echo "ERROR: PR creation succeeded but failed to capture PR number"
              exit 1
            fi

            echo "✅ Created PR #$PR_NUMBER from $BRANCH"
          fi

          echo "pr_number=$PR_NUMBER" >> $GITHUB_OUTPUT

      - name: Auto-merge PR
        if: steps.check_version.outputs.has_dev == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          PR_NUMBER="${{ steps.create_pr.outputs.pr_number }}"

          # Validate PR number exists
          if [ -z "$PR_NUMBER" ]; then
            echo "ERROR: PR number is empty, cannot auto-merge"
            exit 1
          fi

          # Add comment explaining auto-merge
          if ! gh pr comment "$PR_NUMBER" --body "🤖 Auto-merging this automated version change. Tests are skipped for version-only changes."; then
            echo "ERROR: Failed to add comment to PR #$PR_NUMBER"
            exit 1
          fi

          # Auto-merge with admin override (bypasses branch protection)
          # Note: Requires admin permissions or use --auto for standard auto-merge
          if ! gh pr merge "$PR_NUMBER" --squash --admin --delete-branch; then
            echo "ERROR: Failed to auto-merge PR #$PR_NUMBER"
            echo "This may be due to insufficient permissions or branch protection rules"
            echo "Please merge manually: https://github.com/${{ github.repository }}/pull/$PR_NUMBER"

            # Add failure comment to PR
            gh pr comment "$PR_NUMBER" --body "⚠️ Auto-merge failed. Please merge manually." || true

            exit 1
          fi

          # Verify merge completed
          if ! PR_STATE=$(gh pr view "$PR_NUMBER" --json state --jq '.state'); then
            echo "WARNING: Could not verify PR state after merge"
          elif [ "$PR_STATE" != "MERGED" ]; then
            echo "ERROR: PR #$PR_NUMBER merge command succeeded but PR state is $PR_STATE"
            exit 1
          fi

          echo "✅ Auto-merged PR #$PR_NUMBER and deleted branch"
