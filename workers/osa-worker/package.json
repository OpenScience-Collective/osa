{
  "name": "osa-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for OSA security proxy",
  "main": "index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "deploy:dev": "wrangler deploy --env dev",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
