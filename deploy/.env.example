# OSA - Open Science Assistant
# Environment Configuration
#
# Copy this file to .env and configure as needed:
#   cp .env.example .env
#
# Then start with:
#   docker-compose up -d

# =============================================================================
# API Settings
# =============================================================================

# Enable debug mode (exposes /docs and /redoc)
DEBUG=false

# Root path for reverse proxy (e.g., /osa if behind proxy at /osa)
ROOT_PATH=

# API key authentication
REQUIRE_API_AUTH=true
# Comma-separated API keys for authentication
API_KEYS=

# =============================================================================
# LLM Provider Keys (server-side)
# =============================================================================
# At least one provider key is required for the assistant to work

OPENROUTER_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Default model (OpenRouter format: creator/model-name)
DEFAULT_MODEL=openai/gpt-oss-120b
DEFAULT_MODEL_PROVIDER=Cerebras

# =============================================================================
# Knowledge Sync API Keys (optional)
# =============================================================================
# These enable syncing from external sources. Without keys, syncing still works
# but may hit rate limits.

# GitHub token for syncing issues/PRs from HED repositories
# Get one at: https://github.com/settings/tokens
GITHUB_TOKEN=

# Semantic Scholar API key for higher rate limits
# Apply at: https://www.semanticscholar.org/product/api
SEMANTIC_SCHOLAR_API_KEY=

# PubMed/NCBI API key for higher rate limits
# Get one at: https://www.ncbi.nlm.nih.gov/account/settings/
PUBMED_API_KEY=

# =============================================================================
# Knowledge Sync Scheduling
# =============================================================================
# Cron expressions for automated sync jobs

# Enable/disable automated sync (set to false to sync manually only)
SYNC_ENABLED=true

# GitHub sync schedule (default: daily at 2am UTC)
SYNC_GITHUB_CRON=0 2 * * *

# Papers sync schedule (default: weekly on Sunday at 3am UTC)
SYNC_PAPERS_CRON=0 3 * * 0

# =============================================================================
# Observability (optional)
# =============================================================================

LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=https://cloud.langfuse.com
