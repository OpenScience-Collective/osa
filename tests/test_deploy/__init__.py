"""Deployment configuration tests.

These parse the deployment files directly (docker-compose.yml, .env.example)
rather than eyeballing them: they are the files nobody runs until a deploy
breaks.
"""
